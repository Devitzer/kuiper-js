# v0.2.0 (04/14/2026)

New configuration features

## Added
- Basic configuration support (with more advanced features to come)
- Added the changelog
- NPM publishes are now done with provenance via the GitHub action and kuiper-js releases will also be published as a GitHub Release.

## Changes
- `README.md` includes an example of the new config system.

## Fixes
- Made a minor correction to a timestamp example in README.md to follow the ISO 8601 standard.
- Fixed a bug where users couldn't install the package due to the pre-install script missing dependencies.

# v0.1.0 (04/14/2026)

Initial commit, adds the base functions.